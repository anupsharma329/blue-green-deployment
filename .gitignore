# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform in subdirectory
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfstate.backup
terraform/.terraform.tfstate.lock.info
terraform/backend.hcl
terraform/launch-templates-alternative.tf.example

# Optional: ignore alternative example if you don't want it tracked
# terraform/launch-templates-alternative.tf.example

# OS / Editor
.DS_Store
*.swp
*.swo
*~

# Node (if any at root)
node_modules/
